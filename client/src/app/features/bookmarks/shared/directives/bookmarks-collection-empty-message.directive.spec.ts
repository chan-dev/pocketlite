import { BookmarksCollectionEmptyMessageDirective } from './bookmarks-collection-empty-message.directive';

describe('BookmarksCollectionEmptyMessageDirective', () => {
  it('should create an instance', () => {
    const directive = new BookmarksCollectionEmptyMessageDirective();
    expect(directive).toBeTruthy();
  });
});
